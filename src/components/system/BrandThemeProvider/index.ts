/**
 * @fileoverview BrandThemeProvider component exports
 * @component BrandThemeProvider
 *
 * @description
 * Export module for the Four Loop Digital brand theme provider component.
 * Provides Material-UI theme configuration with brand colors and design tokens.
 *
 * @example
 * ```tsx
 * import { BrandThemeProvider } from '@/components/system/BrandThemeProvider';
 *
 * <BrandThemeProvider>
 *   <App />
 * </BrandThemeProvider>
 * ```
 */

export {
  default as BrandThemeProvider,
  colors,
  default,
} from './BrandThemeProvider';
